"use strict"

//variables we are working with
let a = " 101.1 ";
let b = "55";
let c = "402 Stevens";
let d = "Number 5  ";

//using parseInt for each variable displaying the results
console.log("var a with parseInt" + parseInt(a));

//using parseInt for each variable displaying the results
console.log("var b with parseInt" + parseInt(b));

//using parseInt for each variable displaying the results
console.log("var c with parseInt" + parseInt(c));

//using parseInt for each variable displaying the results
console.log("var d with parseInt" + parseInt(d));

//using parseFloat for each variable displaying the results
console.log("var a with parseFloat" + parseFloat(a));

//using parseFloat for each variable displaying the results
console.log("var b with parseFloat" + parseFloat(b));

//using parseFloat for each variable displaying the results
console.log("var c with parseFloat" + parseFloat(c));

//using parseFloat for each variable displaying the results
console.log("var d with parseFloat" + parseFloat(d));

//variables we coverting using Number
let numbera = " 101.1 ";
let numberb = "55";
let numberc = "402 Stevens";
let numberd = "Number 5  ";

//using number for each variable displaying the results
console.log("var a with number" + number(a));
